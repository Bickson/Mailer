

<%= form_for(@email) do |f| %>
  <% if @email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% @email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :subject, class: "control-label" %><br />
    <%= f.text_field :subject, class: "form-control", style: "width: 40%" %>
  </div>
  <div class="form-group">
    <%= f.label :content %><br />
    <%= f.text_area :content, class: "form-control", rows: "15", style: "width: 70%"%>
  </div>
  <div class="actions">
    <%= f.submit "Send Email", class: "btn btn-primary"%>
  </div>
<% end %>
